{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "include": [
    "src/**/*.ts", // Incluye todos los archivos .ts de la carpeta src, excepto los excluidos.
    "src/**/*.d.ts" // **IMPORTANTE:** Incluye explícitamente todos los archivos de declaración de tipos (.d.ts) en src, como el que creamos.
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}
